# API 设置
api:
  api_key: "your api key"
  base_url: "your base url"
  model: "your model name"  # 添加模型名称选项

# 对话设置
dialogue_settings: "dialogue_settings.json"
  # 对话背景
  # 对话轮数

# 输出设置
output_folder: "./"